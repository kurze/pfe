'use strict';

describe('Service: DBGraph', function () {

	// load the service's module
	beforeEach(module('app'));

	// instantiate service
	// var DBGraph;
	// var db;
	// beforeEach(inject(function (_DBGraph_) {
	// 	DBGraph = _DBGraph_;
	// 	db = new ydn.db.Storage('testGraph');
	// }));

	it('access DB', function () {
		// var truc ={
		// 	plop : 'truc',
		// 	num : 12
		// };
		// db.add('testGraph', truc, 0);
		// db.get('testGraph', 0).always(function(record){
		// 	expect(record).toEqual(truc);
		// });
	});


});
